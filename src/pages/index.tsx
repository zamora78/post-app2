import ShowPosts from "../components/Posts";
import ShowDetails from "../components/PostDetails";

export const Posts = () => {
  return <ShowPosts />;
};

export const PostDetails = () => {
  return <ShowDetails />;
};
